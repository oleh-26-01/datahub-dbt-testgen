# Generated by dbt-testgen from DataHub metadata.
# Every test below cites a sentence a human wrote in a DataHub Document.
# 139 further tests were declined for lack of such evidence and are
# listed, with reasons, in EVIDENCE.md. Read that file before adding them
# back by hand.
# Regenerate with:  dbt-testgen generate

version: 2
models:
  - name: order_details
    meta:
      datahub_urn: urn:li:dataset:(urn:li:dataPlatform:dbt,b2fd91.ORDER_ENTRY_DB.analytics.order_details,PROD)
    columns:
      - name: billing_address_line1
        description: First line of customer billing address
        data_type: text
        meta:
          contains_pii: true
      - name: billing_address_line2
        description: Second line of customer billing address
        data_type: text
        meta:
          contains_pii: true
      - name: billing_country
        description: Country name for the billing address
        data_type: text
        meta:
          contains_pii: true
      - name: billing_region
        description: Region/state/province name for the billing address
        data_type: text
        meta:
          contains_pii: true
      - name: billing_town_city
        description: Town or city of customer billing address
        data_type: text
        meta:
          contains_pii: true
      - name: billing_zipcode
        description: Postal code of customer billing address
        data_type: number
        meta:
          contains_pii: true
      - name: category_id
        description: Identifier of the product category
        data_type: number
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('product_categories')
                field: category_id
      - name: condition
        description: Condition of product (e.g., New, Refurbished)
        data_type: text
        data_tests:
          - accepted_values:
              arguments:
                values:
                  - new
                  - refurbished
                  - used
      - name: cust_email
        description: Customer email address
        data_type: text
        meta:
          contains_pii: true
      - name: cust_first_name
        description: Customer first name
        data_type: text
        meta:
          contains_pii: true
      - name: cust_last_name
        description: Customer last name
        data_type: text
        meta:
          contains_pii: true
      - name: customer_class
        description: Classification of the customer (e.g., Retail, Enterprise, Online)
        data_type: text
        data_tests:
          - accepted_values:
              arguments:
                values: &id001
                  - Premium
                  - Standard
      - name: customer_id
        description: Unique identifier for the customer
        data_type: number
        meta:
          contains_pii: true
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('customers')
                field: customer_id
      - name: delivery_type
        description: Shipping method (e.g., Standard, Curbside, Overnight)
        data_type: text
        data_tests:
          - accepted_values:
              arguments:
                values:
                  - ground
                  - express
                  - overnight
                  - pickup
      - name: gift_wrap
        description: Flag or type of gift wrapping requested
        data_type: text
        data_tests:
          - accepted_values:
              arguments:
                values:
                  - Y
                  - N
      - name: order_id
        description: Unique identifier for the order
        data_type: number
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('orders')
                field: order_id
      - name: order_mode
        description: Method of order placement (e.g., online, phone, instore)
        data_type: text
        data_tests:
          - accepted_values:
              arguments:
                values:
                  - online
                  - direct
      - name: order_status
        description: Current status of the order (e.g., 1=Pending, 2=Processing, 3=Shipped)
        data_type: number
        data_tests:
          - accepted_values:
              arguments:
                values: &id002
                  - Pending
                  - Open
                  - Shipped
                  - Complete
                  - Cancelled
                  - On Hold
      - name: order_total
        description: Total monetary value of the order
        data_type: float
        meta:
          contains_pii: true
      - name: payment_method_code
        description: Code indicating payment method used
        data_type: text
        data_tests:
          - accepted_values:
              arguments:
                values:
                  - CreditCard
                  - PurchaseOrder
                  - AccountBalance
      - name: phone_number
        description: Customer contact phone number
        data_type: text
        meta:
          contains_pii: true
      - name: product_id
        description: Identifier of the product ordered
        data_type: number
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('products')
                field: product_id
      - name: product_status
        description: Current status of the product (e.g., Active, Inactive, Backordered)
        data_type: text
        data_tests:
          - accepted_values:
              arguments:
                values: &id003
                  - orderable
                  - planned
                  - under development
                  - obsolete
      - name: promotion_id
        description: Identifier of the promotion applied to the order
        data_type: number
        data_tests:
          - relationships:
              arguments:
                to: ref('promotions')
                field: promotion_id
      - name: shipping_address_line1
        description: First line of customer shipping address
        data_type: text
        meta:
          contains_pii: true
      - name: shipping_address_line2
        description: Second line of customer shipping address
        data_type: text
        meta:
          contains_pii: true
      - name: shipping_country
        description: Country name for the shipping address
        data_type: text
        meta:
          contains_pii: true
      - name: shipping_region
        description: Region/state/province name for the shipping address
        data_type: text
        meta:
          contains_pii: true
      - name: shipping_town_city
        description: Town or city of customer shipping address
        data_type: text
        meta:
          contains_pii: true
      - name: shipping_zipcode
        description: Postal code of customer shipping address
        data_type: number
        meta:
          contains_pii: true
      - name: wait_till_complete_yn
        description: Flag indicating whether to ship complete order or partial (Y/N)
        data_type: text
        data_tests:
          - accepted_values:
              arguments:
                values:
                  - Y
                  - N
      - name: warehouse_id
        description: Identifier of the warehouse fulfilling the order
        data_type: number
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('warehouses')
                field: warehouse_id
  - name: promotions
    meta:
      datahub_urn: urn:li:dataset:(urn:li:dataPlatform:dbt,b2fd91.order_entry_db.order_entry.promotions,PROD)
    columns:
      - name: promotion_id
        description: Unique identifier for the promotion
        data_type: number
        data_tests:
          - not_null
          - unique
  - name: addresses
    meta:
      datahub_urn: urn:li:dataset:(urn:li:dataPlatform:dbt,b2fd91.order_entry_db.order_entry.addresses,PROD)
    columns:
      - name: address_id
        description: Unique identifier for the address
        data_type: number
        meta:
          contains_pii: true
        data_tests:
          - not_null
          - unique
      - name: address_line1
        description: First line of address
        data_type: text
        meta:
          contains_pii: true
      - name: address_line2
        description: Second line of address
        data_type: text
        meta:
          contains_pii: true
      - name: country_id
        description: Reference to country table
        data_type: number
        data_tests:
          - relationships:
              arguments:
                to: ref('countries')
                field: country_id
      - name: customer_id
        description: Foreign key to the customers table
        data_type: number
        meta:
          contains_pii: true
        data_tests:
          - relationships:
              arguments:
                to: ref('customers')
                field: customer_id
      - name: region_id
        description: Reference to region table
        data_type: number
        data_tests:
          - relationships:
              arguments:
                to: ref('regions')
                field: region_id
      - name: town_city
        description: Town or city of the address
        data_type: text
        meta:
          contains_pii: true
      - name: zipcode
        description: Postal code of address
        data_type: number
        meta:
          contains_pii: true
  - name: customers
    meta:
      datahub_urn: urn:li:dataset:(urn:li:dataPlatform:dbt,b2fd91.order_entry_db.order_entry.customers,PROD)
    columns:
      - name: address_line1
        description: First line of customer primary address
        data_type: text
        meta:
          contains_pii: true
      - name: address_line2
        description: Second line of customer primary address
        data_type: text
        meta:
          contains_pii: true
      - name: address_line3
        description: Third line of customer primary address
        data_type: text
        meta:
          contains_pii: true
      - name: country_id
        description: Reference to country table for primary address
        data_type: number
        meta:
          contains_pii: true
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('countries')
                field: country_id
      - name: cust_email
        description: Customer email address
        data_type: text
        meta:
          contains_pii: true
      - name: cust_first_name
        description: Customer first name
        data_type: text
        meta:
          contains_pii: true
      - name: cust_last_name
        description: Customer last name
        data_type: text
        meta:
          contains_pii: true
      - name: customer_class
        description: Classification of the customer (e.g., Platinum, Gold, Silver)
        data_type: text
        data_tests:
          - accepted_values:
              arguments:
                values: *id001
      - name: customer_id
        description: Unique identifier for the customer
        data_type: number
        meta:
          contains_pii: true
        data_tests:
          - not_null
          - unique
      - name: dob
        description: Customer date of birth
        data_type: text
        meta:
          contains_pii: true
      - name: mailshot
        description: Flag indicating if customer has opted for marketing emails (Y/N)
        data_type: number
        data_tests:
          - accepted_values:
              arguments:
                values:
                  - Y
                  - N
      - name: partner_mailshot
        description: Flag indicating if customer has opted for partner marketing (Y/N)
        data_type: number
        data_tests:
          - accepted_values:
              arguments:
                values:
                  - Y
                  - N
      - name: phone_number
        description: Customer contact phone number
        data_type: text
        meta:
          contains_pii: true
      - name: region_id
        description: Reference to region table for primary address
        data_type: number
        meta:
          contains_pii: true
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('regions')
                field: region_id
      - name: town_city
        description: Town or city of customer primary address
        data_type: text
        meta:
          contains_pii: true
      - name: zipcode
        description: Postal code of customer primary address
        data_type: number
        meta:
          contains_pii: true
  - name: inventories
    meta:
      datahub_urn: urn:li:dataset:(urn:li:dataPlatform:dbt,b2fd91.order_entry_db.order_entry.inventories,PROD)
    columns:
      - name: product_id
        description: Foreign key to the products table
        data_type: number
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('products')
                field: product_id
      - name: warehouse_id
        description: Foreign key to the warehouses table
        data_type: number
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('warehouses')
                field: warehouse_id
  - name: order_items
    meta:
      datahub_urn: urn:li:dataset:(urn:li:dataPlatform:dbt,b2fd91.order_entry_db.order_entry.order_items,PROD)
    columns:
      - name: condition
        description: Condition of product (e.g., New, Refurbished)
        data_type: text
        data_tests:
          - accepted_values:
              arguments:
                values:
                  - new
                  - refurbished
                  - used
      - name: gift_wrap
        description: Flag or type of gift wrapping requested
        data_type: text
        data_tests:
          - accepted_values:
              arguments:
                values:
                  - Y
                  - N
      - name: line_item_id
        description: Line item sequence number within the order
        data_type: number
        data_tests:
          - not_null
          - unique
      - name: order_id
        description: Foreign key to the orders table
        data_type: number
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('orders')
                field: order_id
      - name: product_id
        description: Foreign key to the products table
        data_type: number
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('products')
                field: product_id
  - name: orders
    meta:
      datahub_urn: urn:li:dataset:(urn:li:dataPlatform:dbt,b2fd91.order_entry_db.order_entry.orders,PROD)
    columns:
      - name: billing_address_id
        description: Reference to the billing address
        data_type: number
        meta:
          contains_pii: true
        data_tests:
          - not_null
      - name: customer_id
        description: Foreign key to the customers table
        data_type: number
        meta:
          contains_pii: true
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('customers')
                field: customer_id
      - name: delivery_address_id
        description: Reference to the shipping address
        data_type: number
        meta:
          contains_pii: true
        data_tests:
          - not_null
      - name: delivery_type
        description: Shipping method (e.g., Standard, Express, Overnight)
        data_type: text
        data_tests:
          - accepted_values:
              arguments:
                values:
                  - ground
                  - express
                  - overnight
                  - pickup
      - name: order_id
        description: Unique identifier for the order
        data_type: number
        data_tests:
          - not_null
          - unique
      - name: order_mode
        description: Method of order placement (e.g., online, phone, direct)
        data_type: text
        data_tests:
          - accepted_values:
              arguments:
                values:
                  - online
                  - direct
      - name: order_status
        description: Current status of the order (e.g., 1=Pending, 2=Processing, 3=Shipped)
        data_type: number
        data_tests:
          - accepted_values:
              arguments:
                values: *id002
      - name: payment_method_code
        description: Code indicating payment method used
        data_type: text
        data_tests:
          - accepted_values:
              arguments:
                values:
                  - CreditCard
                  - PurchaseOrder
                  - AccountBalance
      - name: promotion_id
        description: Foreign key to promotions table if applicable
        data_type: float
        data_tests:
          - relationships:
              arguments:
                to: ref('promotions')
                field: promotion_id
      - name: wait_till_complete_yn
        description: Flag indicating whether to ship complete order or partial (Y/N)
        data_type: text
        data_tests:
          - accepted_values:
              arguments:
                values:
                  - Y
                  - N
      - name: warehouse_id
        description: Foreign key to warehouses table for fulfillment location
        data_type: number
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('warehouses')
                field: warehouse_id
  - name: product_categories
    meta:
      datahub_urn: urn:li:dataset:(urn:li:dataPlatform:dbt,b2fd91.order_entry_db.order_entry.product_categories,PROD)
    columns:
      - name: category_id
        description: Unique identifier for the product category
        data_type: number
        data_tests:
          - not_null
          - unique
      - name: parent_category_id
        description: Self-referential key to parent category (allows hierarchy)
        data_type: float
        data_tests:
          - relationships:
              arguments:
                to: ref('product_categories')
                field: parent_category_id
  - name: products
    meta:
      datahub_urn: urn:li:dataset:(urn:li:dataPlatform:dbt,b2fd91.order_entry_db.order_entry.products,PROD)
    columns:
      - name: category_id
        description: Foreign key to the product_categories table
        data_type: number
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('product_categories')
                field: category_id
      - name: product_id
        description: Unique identifier for the product
        data_type: number
        data_tests:
          - not_null
          - unique
      - name: product_status
        description: Current status (e.g., Available, Discontinued, Planned)
        data_type: text
        data_tests:
          - accepted_values:
              arguments:
                values: *id003
      - name: supplier_id
        description: ID of the supplier for this product
        data_type: number
        data_tests:
          - not_null
  - name: warehouses
    meta:
      datahub_urn: urn:li:dataset:(urn:li:dataPlatform:dbt,b2fd91.order_entry_db.order_entry.warehouses,PROD)
    columns:
      - name: warehouse_id
        description: Unique identifier for the warehouse
        data_type: number
        data_tests:
          - not_null
          - unique
  - name: order_history
    meta:
      datahub_urn: urn:li:dataset:(urn:li:dataPlatform:dbt,b2fd91.ORDER_ENTRY_DB.analytics.order_history,PROD)
    columns:
      - name: customer_id
        data_type: number
        meta:
          contains_pii: true
        data_tests:
          - relationships:
              arguments:
                to: ref('customers')
                field: customer_id
      - name: order_id
        data_type: number
        data_tests:
          - relationships:
              arguments:
                to: ref('orders')
                field: order_id
